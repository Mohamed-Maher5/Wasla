# Daleel
Arabic AI chatbot that answers agent queries instantly using RAG over your internal knowledge base (SOPs, docs, wikis), with cited sources and role-based access control (Super Admin → Admin → Organization → User).
